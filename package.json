{
  "name": "api-dados",
  "version": "1.0.0",
  "description": "Api para manipulação dos dados dos pacientes no banco de dados.",
  "license": "ISC",
  "author": "Rafael Laube",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch --env-file=.env.local src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "build:clean": "npm run clean && npm run build",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
